version: '3.8'

services:
  adattracker:
    build:
      context: .
      dockerfile: Dockerfile
    image: adattracker-pro:latest
    container_name: adattracker-pro
    restart: unless-stopped
    env_file:
      - .env
    ports:
      # LOCAL / HOMELAB: use "8000:8000"
      # PRODUCTION (Cloudflare Tunnel): use "127.0.0.1:8000:8000" — never expose to 0.0.0.0
      - "${BIND_HOST:-0.0.0.0}:${PORT:-8000}:8000"
    volumes:
      - adattracker_data:/app/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

volumes:
  adattracker_data:
    driver: local
